# Common pitfalls

This doc lists frequent issues when running Dockerized local environments.

Examples:
- ports already in use
- stale containers / old volumes
- `.env` not created
- DB not ready yet (healthcheck timing)
- permission issues with bind mounts (depending on OS)
