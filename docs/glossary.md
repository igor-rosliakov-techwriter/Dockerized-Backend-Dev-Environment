# Glossary

- **Container**: a packaged process running with isolated filesystem/network settings.
- **Image**: a build artifact used to create containers.
- **Docker Compose**: tool to run multi-container applications locally.
- **Healthcheck**: a command Docker runs to detect whether a service is ready.
- **Worker**: background process consuming jobs outside the HTTP request lifecycle.
