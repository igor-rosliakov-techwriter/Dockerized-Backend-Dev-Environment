# Runbook

Operational playbook for typical incidents in a local environment.

Scenarios:
- API container is up but `/health` fails
- Worker is running but jobs are not processed
- DB refuses connections
- Redis is unreachable
- Resetting state safely
